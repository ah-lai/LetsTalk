import { Input } from 'antd';
import React from 'react'

class MessageFormSubmit extends React.Component {
    render() {
        return (
            <form>
                <Input placeholder="Submit Message" />
            </form>
        )
    }
}

export default MessageFormSubmit